export default function Home() {
  return (
    <main style={{ fontFamily: 'system-ui', padding: 24 }}>
      <h1>Rule Engine</h1>
      <p>Management API under <code>/api/v1/*</code>. Execute lives on Fastify (Phase 4).</p>
    </main>
  );
}
